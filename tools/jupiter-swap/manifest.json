{
  "name": "jupiter-swap",
  "description": "Quotes and builds Solana token swap transactions using the Jupiter aggregator. Requests the user's wallet address from the coordinator, fetches the best route and quote, then returns both the quote details and a serialized transaction for the coordinator to sign and submit via the Solana capability.",
  "version": "0.1.3",
  "executable": "./bin/jupiter-swap",
  "required_capabilities": ["network-manager"],
  "env": [
    { "name": "JUPITER_API_KEY",        "sensitive": true,  "required": false },
    { "name": "JUPITER_RATE_LIMIT_RPS", "sensitive": false, "required": false }
  ],
  "timeout": "120s",
  "llm_options": {
    "primary": "gemini-2.5-pro",
    "supported": [
      { "model": "gemini-2.5-pro",  "provider": "google"    },
      { "model": "gpt-4o",          "provider": "openai"    },
      { "model": "claude-opus-4-6", "provider": "anthropic" }
    ]
  }
}
